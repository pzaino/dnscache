[package]
name = "dnscache"
version = "0.1.4"
edition = "2024"

[dependencies]
dashmap = "6"
num_cpus = "1"
serde = { version = "1", features = ["derive"] }
toml = "1.0"
rand = { version = "0.8", features = ["std"] }

[profile.release]
overflow-checks = true
