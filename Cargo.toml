[package]
name = "dnscache"
version = "0.1.3"
edition = "2024"

[dependencies]
dashmap = "6"
num_cpus = "1"
serde = { version = "1", features = ["derive"] }
toml = "1.0"

[profile.release]
overflow-checks = true
