[Unit]
Description=Rust DNS Cache
After=network.target

[Service]
ExecStart=/usr/local/bin/dnscache
Restart=always
User=root
AmbientCapabilities=CAP_NET_BIND_SERVICE
NoNewPrivileges=true

[Install]
WantedBy=multi-user.target
